library(fastTopics)
library(Matrix)
set.seed(1)
X <- matrix(rpois(200 * 50, 1), 200, 50)
fit <- fit_topic_model(X, k = 3, numiter.main = 10, verbose = "none")
cat("class:", class(fit), "\n")
cat("names:", paste(names(fit), collapse = ", "), "\n")
ll <- loglik_multinom_topic_model(X, fit)
cat("loglik:", ll, "\n")
